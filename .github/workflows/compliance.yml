name: compliance

on:
  push:
  pull_request:

jobs:
  taint-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: OSS taint marker scan
        run: ./scripts/oss-taint-scan.sh
