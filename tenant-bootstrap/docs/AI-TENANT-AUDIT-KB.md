# AI Tenant Audit KB

Tenant-specific audit notes were removed.

Use this file only for generic operator guidance:

- record the tenant label, domain, and role used
- list the commands run
- link only to local run artifacts that are safe to keep
- avoid real tenant ids, domains, user emails, app ids, and secrets

Keep live evidence in local output folders, not in tracked docs.
