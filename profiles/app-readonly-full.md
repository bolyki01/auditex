# app-readonly-full

- auth mode: customer-local app registration
- expected app mode: read-only only
- default collectors: `identity`, `security`, `intune`, `teams`, `exchange`
- purpose: secondary pass for deeper evidence when delegated mode is blocked
