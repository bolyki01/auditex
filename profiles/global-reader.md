# global-reader

- auth mode: delegated preferred
- expected delegated role: `Global Reader`
- default collectors: `identity`, `security`, `intune`, `teams`
- optional escalation: read-only app permissions for blocked workloads
- purpose: standard first-pass customer audit without app consent
